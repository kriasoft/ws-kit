import{x as s,b as i,i as t,p as o}from"./chunks/framework.Dl5MHC_T.js";const g=JSON.parse('{"title":"Development Rules","description":"","frontmatter":{},"headers":[],"relativePath":"specs/rules.md","filePath":"specs/rules.md","lastUpdated":1763674706000}'),r={name:"specs/rules.md"};function a(n,e,l,c,d,p){return t(),i("div",null,[...e[0]||(e[0]=[o(`<h1 id="development-rules" tabindex="-1">Development Rules <a class="header-anchor" href="#development-rules" aria-label="Permalink to “Development Rules”">​</a></h1><p><strong>Quick Lookup Index for MUST/NEVER/ALWAYS rules.</strong></p><p>This is a <strong>rapid reference</strong> to help you find rules quickly. For implementation details, rationale, and complete guidance, always follow the linked canonical specs.</p><p><strong>Important</strong>: This file is an INDEX and QUICK REFERENCE, not a canonical source. The detailed specs (schema.md, router.md, validation.md, etc.) are authoritative. When guidance seems incomplete here, that&#39;s intentional—click the linked specs for full context and rationale.</p><p><strong>How to Use:</strong></p><ol><li><strong>Quickly find rules</strong>: Scan this index for your use case</li><li><strong>Get implementation details</strong>: Click the linked <code>docs/specs/spec.md#section</code> references</li><li><strong>Understand trade-offs</strong>: Read the referenced spec section for &quot;why&quot; and detailed examples</li><li><strong>When in doubt</strong>: The linked canonical section always takes precedence</li></ol><p><strong>What&#39;s NOT here</strong>: Design rationale, code examples (beyond one-liners), trade-off analysis. Read the linked specs for those.</p><hr><h2 id="critical-rules-never-violate" tabindex="-1">Critical Rules (NEVER Violate) <a class="header-anchor" href="#critical-rules-never-violate" aria-label="Permalink to “Critical Rules (NEVER Violate)”">​</a></h2><h3 id="type-safety" tabindex="-1">Type Safety <a class="header-anchor" href="#type-safety" aria-label="Permalink to “Type Safety”">​</a></h3><ul><li><strong>NEVER</strong> access <code>ctx.payload</code> if schema doesn&#39;t define it (TypeScript will error) → ADR-001</li><li><strong>ALWAYS</strong> rely on TypeScript to enforce <code>ctx.payload</code> presence via conditional typing → ADR-001</li></ul><h3 id="import-patterns-adr-007-export-with-helpers" tabindex="-1">Import Patterns (ADR-007: Export-with-Helpers) <a class="header-anchor" href="#import-patterns-adr-007-export-with-helpers" aria-label="Permalink to “Import Patterns (ADR-007: Export-with-Helpers)”">​</a></h3><p>See <a href="./schema#canonical-import-patterns">docs/specs/schema.md#Canonical-Import-Patterns</a> for complete patterns and examples.</p><p><strong>MUST enforce:</strong></p><ul><li><strong>ALWAYS</strong> use single canonical import source (prevents dual package hazard) → ADR-007</li><li><strong>NEVER</strong> import directly from <code>zod</code> or <code>valibot</code>; use <code>@ws-kit/zod</code> or <code>@ws-kit/valibot</code></li></ul><p><strong>Recommended ESLint rule:</strong></p><div class="language-javascript"><button title="Copy Code" class="copy"></button><span class="lang">javascript</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;no-restricted-imports&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;error&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  patterns: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { group: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;zod&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], message: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Use @ws-kit/zod instead&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    { group: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;valibot&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">], message: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Use @ws-kit/valibot instead&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ],</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}]</span></span></code></pre></div><h3 id="runtime-selection-adr-006" tabindex="-1">Runtime Selection (ADR-006) <a class="header-anchor" href="#runtime-selection-adr-006" aria-label="Permalink to “Runtime Selection (ADR-006)”">​</a></h3><p><strong>Production deployments MUST declare platform explicitly:</strong></p><ul><li><strong>ALWAYS</strong> use platform-specific package and <code>serve()</code> function → ADR-006 <ul><li>Bun: <code>import { serve } from &quot;@ws-kit/bun&quot;</code></li><li>Cloudflare DO: High-level <code>serve()</code> or low-level <code>createDurableObjectHandler()</code></li></ul></li><li><strong>NEVER</strong> rely on auto-detection → ADR-006</li></ul><p><strong>Examples:</strong></p><div class="language-typescript"><button title="Copy Code" class="copy"></button><span class="lang">typescript</span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ✅ Bun (recommended for Bun deployments)</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { serve } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;@ws-kit/bun&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">serve</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(router, { port: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> });</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// ✅ Cloudflare Durable Objects</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { createDurableObjectHandler } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;@ws-kit/cloudflare&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> handler</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> createDurableObjectHandler</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(router);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">  fetch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">req</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Request</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">state</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> DurableObjectState</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">env</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">:</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Env</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> handler.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">fetch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(req);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  },</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">};</span></span></code></pre></div><h3 id="security-validation" tabindex="-1">Security &amp; Validation <a class="header-anchor" href="#security-validation" aria-label="Permalink to “Security &amp; Validation”">​</a></h3><ul><li><strong>NEVER</strong> re-validate in handlers (trust schema) → docs/specs/validation.md#Flow</li><li><strong>ALWAYS</strong> use strict schemas (reject unknown keys) → docs/specs/schema.md#Strict-Schemas</li></ul><h3 id="identity-reserved-keys-critical-for-security" tabindex="-1">Identity &amp; Reserved Keys (Critical for Security) <a class="header-anchor" href="#identity-reserved-keys-critical-for-security" aria-label="Permalink to “Identity &amp; Reserved Keys (Critical for Security)”">​</a></h3><ul><li><strong>NEVER</strong> access <code>clientId</code> via <code>ctx.meta.clientId</code> — server generates it; use <code>ctx.clientId</code> instead → docs/specs/validation.md#normalization-rules</li><li><strong>NEVER</strong> allow clients to set reserved keys (<code>clientId</code>, <code>receivedAt</code>) — routers strip them during normalization → docs/specs/validation.md#normalization-rules</li><li><strong>NEVER</strong> trust client-provided <code>receivedAt</code> timestamps — use <code>ctx.receivedAt</code> for authoritative server time → docs/specs/schema.md#Which-timestamp-to-use</li><li><strong>ALWAYS</strong> access identity via <code>ctx.clientId</code> in handlers and middleware → docs/specs/router.md#Type-Safe-Sending</li></ul><hr><h2 id="required-patterns-always-use" tabindex="-1">Required Patterns (ALWAYS Use) <a class="header-anchor" href="#required-patterns-always-use" aria-label="Permalink to “Required Patterns (ALWAYS Use)”">​</a></h2><h3 id="validation-flow" tabindex="-1">Schema &amp; Validation <a class="header-anchor" href="#validation-flow" aria-label="Permalink to “Schema &amp; Validation”">​</a></h3><ul><li><strong>ALWAYS</strong> follow validation flow (Parse → Type Check → Lookup → Normalize → Validate → Middleware → Handler) → docs/specs/validation.md#Flow</li><li><strong>ALWAYS</strong> normalize before validate (strip reserved keys BEFORE schema) → docs/specs/validation.md#normalization-rules</li><li><strong>ALWAYS</strong> register RPC schemas with a response descriptor; events MUST NOT declare a response → docs/specs/schema.md#messagedescriptor-validation-contract</li></ul><h3 id="middleware" tabindex="-1">Middleware <a class="header-anchor" href="#middleware" aria-label="Permalink to “Middleware”">​</a></h3><ul><li><strong>ALWAYS</strong> use <code>router.use(middleware)</code> for global middleware → docs/specs/router.md#Middleware</li><li><strong>ALWAYS</strong> use <code>router.route(schema).use(middleware).on(handler)</code> for per-route middleware → docs/specs/router.md#Middleware</li><li><strong>ALWAYS</strong> call <code>next()</code> to continue to next middleware or handler → docs/specs/router.md#Middleware</li><li><strong>ALWAYS</strong> skip calling <code>next()</code> to prevent handler execution → docs/specs/router.md#Middleware</li><li><strong>ALWAYS</strong> register global middleware before per-route middleware → docs/specs/router.md#Middleware</li></ul><h3 id="connection-context" tabindex="-1">Connection &amp; Context <a class="header-anchor" href="#connection-context" aria-label="Permalink to “Connection &amp; Context”">​</a></h3><ul><li><strong>ALWAYS</strong> access identity via <code>ctx.clientId</code> → docs/specs/validation.md#Reserved-Meta-Keys</li><li><strong>ALWAYS</strong> use <code>ctx.receivedAt</code> for server logic → docs/specs/schema.md#Which-timestamp-to-use</li><li><strong>ALWAYS</strong> use <code>ctx.assignData(partial)</code> to merge connection data updates (write-partial pattern) → docs/specs/router.md#Modifying-Connection-Data</li><li><strong>NEVER</strong> mutate <code>ctx.data</code> directly; use <code>assignData()</code> → docs/specs/router.md#Modifying-Connection-Data</li></ul><h3 id="type-system" tabindex="-1">Type System <a class="header-anchor" href="#type-system" aria-label="Permalink to “Type System”">​</a></h3><ul><li><strong>ALWAYS</strong> use intersection types for conditional <code>payload</code> → ADR-001</li></ul><h3 id="error-handling" tabindex="-1">Error Handling <a class="header-anchor" href="#error-handling" aria-label="Permalink to “Error Handling”">​</a></h3><ul><li><strong>ALWAYS</strong> use <code>ctx.error(code, message, details)</code> for type-safe error responses → ADR-009, docs/specs/error-handling.md</li><li><strong>ALWAYS</strong> wrap async ops in try/catch → docs/specs/error-handling.md</li><li><strong>ALWAYS</strong> keep connections open (handler must close explicitly) → docs/specs/error-handling.md</li><li><strong>ALWAYS</strong> log errors with <code>clientId</code> for traceability → docs/specs/error-handling.md</li><li><strong>ALWAYS</strong> implement <code>onError</code> hook in <code>serve()</code> for centralized error handling → ADR-009</li><li><strong>NEVER</strong> include passwords, tokens, API keys, or credentials in error details (automatically stripped) → docs/specs/error-handling.md#Error-Detail-Sanitization</li><li><strong>ALWAYS</strong> treat <a href="#connection-close-policy">Connection Close Policy</a> as authoritative source: (1) enumerate all auto-close cases in policy table, (2) test with explicit close codes, (3) link from all related sections → docs/specs/error-handling.md#connection-close-policy</li></ul><h3 id="messaging" tabindex="-1">Messaging <a class="header-anchor" href="#messaging" aria-label="Permalink to “Messaging”">​</a></h3><ul><li><strong>ALWAYS</strong> use <code>ctx.send()</code> for unicast → docs/specs/router.md#Type-Safe-Sending</li><li><strong>ALWAYS</strong> use <code>router.publish()</code> for multicast (validates before broadcast) → docs/specs/pubsub.md</li><li><strong>ALWAYS</strong> use <code>ctx.error()</code> for sending error messages to clients → docs/specs/error-handling.md</li><li><strong>NEVER</strong> inject <code>clientId</code> into meta (include sender in payload/meta instead) → docs/specs/pubsub.md#9.6-Origin-Tracking</li><li><strong>ALWAYS</strong> auto-inject <code>timestamp</code> in outbound messages → docs/specs/router.md#Type-Safe-Sending</li><li><strong>NEVER</strong> send <code>ERROR</code> from clients unless implementing custom protocol (server-to-client by default) → docs/specs/error-handling.md#Error-Message-Direction</li></ul><h3 id="lifecycle-hooks-adr-009" tabindex="-1">Lifecycle Hooks (ADR-009) <a class="header-anchor" href="#lifecycle-hooks-adr-009" aria-label="Permalink to “Lifecycle Hooks (ADR-009)”">​</a></h3><ul><li><strong>ALWAYS</strong> implement lifecycle hooks in <code>serve()</code> options for observability → ADR-009, docs/specs/router.md#Lifecycle-Hooks <ul><li><code>onUpgrade(req)</code> — Before authentication (connection setup)</li><li><code>onOpen(ctx)</code> — After authentication (safe to send messages)</li><li><code>onClose(ctx)</code> — After disconnect (cleanup)</li><li><code>onError(error, ctx)</code> — Centralized error handling</li><li><code>onBroadcast(message, topic)</code> — Track broadcast events</li></ul></li><li><strong>ALWAYS</strong> unsubscribe in <code>onClose()</code> → docs/specs/pubsub.md#9.7-Room-Management</li><li><strong>ALWAYS</strong> store topic IDs in <code>ctx.data</code> → docs/specs/pubsub.md#9.7-Room-Management</li><li><strong>NEVER</strong> throw in lifecycle hooks; errors are caught and logged → ADR-009</li></ul><h3 id="pub-sub-operations" tabindex="-1">Pub/Sub Operations <a class="header-anchor" href="#pub-sub-operations" aria-label="Permalink to “Pub/Sub Operations”">​</a></h3><h4 id="configuration-one-extension-point-only" tabindex="-1">Configuration: One Extension Point Only <a class="header-anchor" href="#configuration-one-extension-point-only" aria-label="Permalink to “Configuration: One Extension Point Only”">​</a></h4><ul><li><p><strong>NEVER</strong> configure pub/sub authorization, normalization, or lifecycle hooks in the router constructor → docs/specs/pubsub.md#5.0-configuration--middleware--policy-split</p><ul><li>Router constructor is <strong>structural shape only</strong>: <code>limits.topicPattern</code>, <code>limits.maxTopicLength</code>, <code>limits.maxTopicsPerConnection</code></li><li>All context-aware policy goes in <strong><code>usePubSub()</code> middleware ONLY</strong></li></ul></li><li><p><strong>ALWAYS</strong> use <code>usePubSub()</code> middleware for ALL context-aware pub/sub logic → docs/specs/pubsub.md#5.0-configuration--middleware--policy-split</p><ul><li>Authorization (<code>authorizeSubscribe</code>, <code>authorizePublish</code>)</li><li>Normalization (<code>normalize</code>)</li><li>Lifecycle telemetry (<code>onSubscribe</code>, <code>onUnsubscribe</code>)</li><li>Cache invalidation (<code>invalidateAuth</code>)</li></ul></li><li><p><strong>ALWAYS</strong> put structural validation in <code>router.limits</code>, NEVER in middleware → docs/specs/pubsub.md#5.0-configuration--middleware--policy-split</p><ul><li>Format pattern: <code>topicPattern</code> (regex)</li><li>Length limits: <code>maxTopicLength</code> (number)</li><li>Per-connection quotas: <code>maxTopicsPerConnection</code> (number)</li></ul></li></ul><h4 id="operation-semantics-canonical-order-idempotency" tabindex="-1">Operation Semantics: Canonical Order &amp; Idempotency <a class="header-anchor" href="#operation-semantics-canonical-order-idempotency" aria-label="Permalink to “Operation Semantics: Canonical Order &amp; Idempotency”">​</a></h4><ul><li><p><strong>ALWAYS</strong> follow the Canonical Operation Order for all subscription operations → docs/specs/pubsub.md#6.1-canonical-operation-order-normative</p><ul><li>Apply in strict sequence: normalize → await in-flight → idempotency check → validate → authorize → limit check → adapter call → mutate → lifecycle hooks</li><li>Applies uniformly to <code>subscribe()</code>, <code>unsubscribe()</code>, <code>subscribeMany()</code>, <code>unsubscribeMany()</code>, and <code>replace()</code></li></ul></li><li><p><strong>🔴 NEVER</strong> validate/authorize/call-adapter on idempotent calls → docs/specs/pubsub.md#6.1-canonical-operation-order-normative</p><ul><li>Duplicate calls return immediately with ZERO side effects (zero validation, zero auth, zero adapter, zero hooks)</li><li>Within batches: already-in-target-state topics are skipped entirely</li></ul></li><li><p><strong>ALWAYS</strong> authorize on normalized topic, never raw input → docs/specs/pubsub.md#6.1-canonical-operation-order-normative, step 5</p></li><li><p><strong>ALWAYS</strong> call adapter before mutating local state → docs/specs/pubsub.md#6.1-canonical-operation-order-normative, step 7</p></li></ul><hr><h2 id="type-system-trade-offs" tabindex="-1">Type System Trade-offs <a class="header-anchor" href="#type-system-trade-offs" aria-label="Permalink to “Type System Trade-offs”">​</a></h2><p>Accept these TypeScript violations for better DX:</p><ul><li><strong>LSP variance in <code>merge()</code></strong> → ADR-001</li><li><strong><code>| any</code> in <code>merge()</code></strong> (allows derived router instances) → ADR-001</li><li><strong><code>@ts-expect-error</code> in type overrides</strong> (enables IDE inference) → ADR-001</li></ul><hr><h2 id="performance" tabindex="-1">Performance Requirements <a class="header-anchor" href="#performance" aria-label="Permalink to “Performance Requirements”">​</a></h2><ul><li><strong>UUID v7 for <code>clientId</code></strong> (time-ordered, better DB indexing)</li><li><strong>Map-based handler lookup</strong> (O(1) by message type)</li><li><strong>Last-write-wins registration</strong> (log warnings on overwrite)</li><li><strong>Single handler per type</strong> (server only; client supports multi-handler)</li><li><strong>Normalization in hot path</strong> (strip reserved keys inline; O(k) where k≤3)</li></ul><hr><h2 id="state-layering" tabindex="-1">State Layering <a class="header-anchor" href="#state-layering" aria-label="Permalink to “State Layering”">​</a></h2><h3 id="connection-state-ctx-data" tabindex="-1">Connection State (<code>ctx.data</code>) <a class="header-anchor" href="#connection-state-ctx-data" aria-label="Permalink to “Connection State (ctx.data)”">​</a></h3><ul><li><code>clientId</code>: Generated during upgrade (UUID v7), NOT in message <code>meta</code></li><li>Custom session data (userId, roles, etc.)</li></ul><h3 id="message-state-ctx-meta" tabindex="-1">Message State (<code>ctx.meta</code>) <a class="header-anchor" href="#message-state-ctx-meta" aria-label="Permalink to “Message State (ctx.meta)”">​</a></h3><ul><li><code>correlationId</code>: Client-controlled request/response correlation</li><li><code>timestamp</code>: Producer time (client&#39;s clock, optional)</li><li>Extended meta fields from schema</li></ul><h3 id="server-context-ctx" tabindex="-1">Server Context (<code>ctx</code>) <a class="header-anchor" href="#server-context-ctx" aria-label="Permalink to “Server Context (ctx)”">​</a></h3><ul><li><code>receivedAt</code>: Server receive timestamp (authoritative, separate from <code>meta.timestamp</code>)</li></ul><hr><h2 id="reserved-keys" tabindex="-1">Reserved Server-Only Keys <a class="header-anchor" href="#reserved-keys" aria-label="Permalink to “Reserved Server-Only Keys”">​</a></h2><p><strong>CANONICAL LIST</strong> (see docs/specs/validation.md#normalization-rules for implementation):</p><ul><li><code>clientId</code>: Connection identity (access via <code>ctx.clientId</code>)</li><li><code>receivedAt</code>: Server receive timestamp (access via <code>ctx.receivedAt</code>)</li></ul><p><strong>Enforcement:</strong></p><ul><li>Routers MUST strip reserved keys during normalization (security boundary) → docs/specs/validation.md#normalization-rules</li><li>Schema creation MUST reject extended meta defining reserved keys (fails fast at design time) → docs/specs/schema.md#Reserved-Server-Only-Meta-Keys</li></ul><hr><h2 id="client-side-constraints" tabindex="-1">Client-Side Constraints <a class="header-anchor" href="#client-side-constraints" aria-label="Permalink to “Client-Side Constraints”">​</a></h2><blockquote><p>Applies to browser/Node client (<code>@ws-kit/client</code>)</p></blockquote><h3 id="message-normalization-outbound" tabindex="-1">Message Normalization (Outbound) <a class="header-anchor" href="#message-normalization-outbound" aria-label="Permalink to “Message Normalization (Outbound)”">​</a></h3><ul><li><strong>ALWAYS</strong> strip reserved/managed keys (<code>clientId</code>, <code>receivedAt</code>, <code>correlationId</code>) from <code>opts.meta</code> → docs/specs/client.md#client-normalization</li><li><strong>ALWAYS</strong> merge meta in order: <code>{ timestamp: Date.now(), ...sanitizedUserMeta, correlationId }</code> → docs/specs/client.md#client-normalization</li><li><strong>ALWAYS</strong> provide <code>correlationId</code> via <code>opts.correlationId</code> (NOT <code>opts.meta.correlationId</code>) → docs/specs/client.md#client-normalization</li><li><strong>ALWAYS</strong> auto-generate <code>correlationId</code> for <code>request()</code> if absent → docs/specs/client.md#client-normalization</li></ul><h3 id="connection-behavior" tabindex="-1">Connection Behavior <a class="header-anchor" href="#connection-behavior" aria-label="Permalink to “Connection Behavior”">​</a></h3><ul><li><strong>ALWAYS</strong> reject pending <code>request()</code> on close (<code>ConnectionClosedError</code>) → docs/specs/client.md#Error-Contract</li><li><strong>ALWAYS</strong> reject <code>request()</code> with <code>StateError</code> when <code>state !== &quot;open&quot;</code> and <code>queue: &quot;off&quot;</code> → docs/specs/client.md#Error-Contract</li><li><strong>NEVER</strong> auto-retry <code>send()</code> or <code>request()</code> after close (at-most-once delivery) → docs/specs/client.md</li><li><strong>ALWAYS</strong> start timeout AFTER message flush on OPEN socket (not when queued) → docs/specs/client.md#request-timeout</li><li><strong>ALWAYS</strong> make <code>close()</code> fully idempotent (never throw/reject due to state) → docs/specs/client.md#Error-Contract</li></ul><h3 id="request-response-correlation" tabindex="-1">Request/Response Correlation <a class="header-anchor" href="#request-response-correlation" aria-label="Permalink to “Request/Response Correlation”">​</a></h3><ul><li><strong>ALWAYS</strong> reject <code>request()</code> with <code>ValidationError</code> when reply has wrong type (matching <code>correlationId</code>) → docs/specs/client.md#Correlation</li><li><strong>ALWAYS</strong> reject <code>request()</code> with <code>ServerError</code> when reply type is <code>ERROR</code> (matching <code>correlationId</code>) → docs/specs/client.md#Correlation</li><li><strong>ALWAYS</strong> drop duplicate replies silently (only first settles promise) → docs/specs/client.md#Correlation</li><li><strong>ALWAYS</strong> reject <code>request()</code> with <code>StateError</code> when <code>opts.signal.aborted === true</code> before dispatch → docs/specs/client.md#request-timeout</li><li><strong>ALWAYS</strong> clean up AbortSignal listeners automatically → docs/specs/client.md#request-timeout</li></ul><h3 id="client-error-contract" tabindex="-1">Client Error Contract <a class="header-anchor" href="#client-error-contract" aria-label="Permalink to “Client Error Contract”">​</a></h3><ul><li><strong>Synchronous throws (<code>TypeError</code>)</strong>: Only during setup/preflight validation → docs/specs/client.md#Error-Contract</li><li><strong>Fire-and-forget (<code>send()</code>)</strong>: NEVER throws; returns <code>boolean</code> → docs/specs/client.md#fire-and-forget-return</li><li><strong>Promise-based methods</strong>: NEVER throw synchronously; return <code>Promise</code> that may reject → docs/specs/client.md#Error-Contract</li><li><strong>StateError</strong>: ALWAYS a Promise rejection, NEVER a synchronous throw → docs/specs/client.md#Error-Contract</li></ul><h3 id="inbound-message-routing" tabindex="-1">Inbound Message Routing <a class="header-anchor" href="#inbound-message-routing" aria-label="Permalink to “Inbound Message Routing”">​</a></h3><ul><li><strong>ALWAYS</strong> route schema handlers BEFORE <code>onUnhandled()</code> hook → docs/specs/client.md#message-processing-order</li><li><strong>NEVER</strong> pass invalid messages to <code>onUnhandled()</code> (drop at validation) → docs/specs/client.md#message-processing-order</li><li><strong>ALWAYS</strong> treat messages in <code>onUnhandled()</code> as readonly → docs/specs/client.md#message-processing-order</li></ul><hr><h2 id="server-client-asymmetry" tabindex="-1">Server vs Client Patterns <a class="header-anchor" href="#server-client-asymmetry" aria-label="Permalink to “Server vs Client Patterns”">​</a></h2><h3 id="handler-registration" tabindex="-1">Handler Registration <a class="header-anchor" href="#handler-registration" aria-label="Permalink to “Handler Registration”">​</a></h3><table tabindex="0"><thead><tr><th>Context</th><th>Behavior</th><th>Rationale</th></tr></thead><tbody><tr><td>Server</td><td>Last-write-wins (warns on overwrite)</td><td>Single authoritative handler per message type; simplified routing</td></tr><tr><td>Client</td><td>Multi-handler (registration order)</td><td>Fan-out pattern common in UI; composability across modules</td></tr></tbody></table><p>See docs/specs/client.md#Multiple-Handlers for client multi-handler semantics.</p><hr><h2 id="route-composition-patterns" tabindex="-1">Route Composition Patterns <a class="header-anchor" href="#route-composition-patterns" aria-label="Permalink to “Route Composition Patterns”">​</a></h2><ul><li><strong>Use <code>merge()</code></strong> for feature module composition</li><li><strong>Pass auth/session data during <code>upgrade()</code></strong></li><li><strong>Store connection metadata in <code>ctx.data</code></strong> for lifecycle cleanup</li></ul><hr><h2 id="server-side-development-patterns" tabindex="-1">Server-Side Development Patterns <a class="header-anchor" href="#server-side-development-patterns" aria-label="Permalink to “Server-Side Development Patterns”">​</a></h2><ul><li><strong>Use <code>message()</code> helper</strong> (export-with-helpers pattern) for type-safe schema creation → ADR-007</li><li><strong>Use <code>createRouter()</code></strong> to create a new router instance → ADR-007</li><li><strong>Use <code>ctx.assignData()</code></strong> for incremental connection data updates (write-partial pattern) → docs/specs/router.md#Modifying-Connection-Data</li><li><strong>Use <code>ctx.error()</code></strong> for type-safe error responses with discriminated union codes → ADR-009</li><li><strong>Use <code>ctx.send()</code></strong> for type-safe unicast messaging to the current client → ADR-020</li><li><strong>Use lifecycle hooks</strong> (<code>onOpen</code>, <code>onClose</code>, <code>onError</code>, etc.) for observability → ADR-009</li></ul><h2 id="client-side-development-patterns" tabindex="-1">Client-Side Development Patterns <a class="header-anchor" href="#client-side-development-patterns" aria-label="Permalink to “Client-Side Development Patterns”">​</a></h2><ul><li><strong>Use <code>message()</code> helper</strong> (export-with-helpers pattern) for schema creation → ADR-007</li><li><strong>Use typed clients</strong> (<code>@ws-kit/client/zod</code> or <code>@ws-kit/client/valibot</code>) for full type inference → ADR-002</li><li><strong>Share schemas</strong> between client and server (single source of truth) → docs/specs/client.md#Sharing-Schemas-Between-Client-and-Server</li><li><strong>Validate before sending</strong> with strict mode (schemas work client-side) → docs/specs/validation.md</li><li><strong>Use <code>request()</code></strong> for RPC-style request/response with automatic correlationId and timeout → docs/specs/client.md#Public-API</li><li><strong>Use <code>send()</code></strong> for fire-and-forget (returns boolean, never throws) → docs/specs/client.md#fire-and-forget-return</li><li><strong>Track broadcasts</strong> — Include sender identity in payload or meta for audit → docs/specs/pubsub.md#9.6-Origin-Tracking</li></ul>`,94)])])}const u=s(r,[["render",a]]);export{g as __pageData,u as default};
