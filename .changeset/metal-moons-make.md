---
"@ws-kit/core": minor
---

feature: add AbortController support to RPC with abort reasons (client-abort, disconnect, idle-timeout); refactor excludeSelf to throw error instead of silently ignoring
