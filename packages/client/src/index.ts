/**
 * @ws-kit/client - Universal WebSocket client
 *
 * Universal client for browsers and Node.js providing:
 * - createClient() factory (validator-agnostic)
 * - Auto-reconnection with exponential backoff
 * - Message queueing and request/response patterns
 * - Authentication helpers
 * - Optional validator integration sub-packages
 *
 * Implementation coming in Phase 2.
 */

export {};
