/**
 * @ws-kit/core - Platform-agnostic WebSocket router and types
 *
 * This is the foundation package providing:
 * - Platform-agnostic WebSocketRouter class
 * - Abstract adapter interfaces (ValidatorAdapter, PlatformAdapter)
 * - PubSub interface with MemoryPubSub default
 * - Message context and server WebSocket type definitions
 * - Lifecycle hooks and configuration types
 *
 * Implementation coming in Phase 2.
 */

export {};
